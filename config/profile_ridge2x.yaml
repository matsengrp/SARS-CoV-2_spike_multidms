# Profile: double beta0_ridge experiment, CPU-only
# Simulation: 1.0e-5 -> 2.0e-5
# Spike: 0 -> 2.0e-5 (matching simulation's doubled value)
gpu_ids: null
n_processes: 48

simulation:
  fitting:
    beta0_ridge: 2.0e-5

spike:
  fitting:
    beta0_ridge: 2.0e-5
